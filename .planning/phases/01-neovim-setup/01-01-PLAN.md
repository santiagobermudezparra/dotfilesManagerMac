---
phase: 01-neovim-setup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - dot_config/nvim/init.lua
  - dot_config/nvim/lua/config/lazy.lua
  - dot_config/nvim/lua/config/options.lua
  - dot_config/nvim/lua/config/keymaps.lua
  - dot_config/nvim/lua/plugins/colorscheme.lua
  - dot_config/nvim/lua/plugins/ui.lua
  - dot_config/nvim/lua/plugins/completion.lua
autonomous: true
requirements:
  - DFM-01
  - DFM-02
  - DFM-04
  - NVIM-01
  - NVIM-02
  - NVIM-03
  - NVIM-04
  - NVIM-05
  - NVIM-06
  - NVIM-07

must_haves:
  truths:
    - "chezmoi source directory structure exists at repo root with dot_config/nvim/"
    - "Neovim starts without errors using the LazyVim bootstrap"
    - "gruvbox-material colorscheme is applied on startup"
    - "Custom keymaps are loaded (scroll centering, telescope symbols)"
    - "Custom options are applied (no line numbers, scrolloff=8, mouse, smartcase, signcolumn=yes)"
    - "blink.cmp provides completion with LSP, snippet, buffer, path sources"
    - "Telescope fuzzy finder is available via keymaps"
    - "nvim-tree file explorer is available"
  artifacts:
    - path: "dot_config/nvim/init.lua"
      provides: "LazyVim bootstrap entry point"
      contains: "require.*config.lazy"
    - path: "dot_config/nvim/lua/config/options.lua"
      provides: "Editor options"
      contains: "scrolloff"
    - path: "dot_config/nvim/lua/config/keymaps.lua"
      provides: "Custom keymaps"
      contains: "vim.keymap.set"
    - path: "dot_config/nvim/lua/plugins/colorscheme.lua"
      provides: "gruvbox-material colorscheme"
      contains: "gruvbox-material"
    - path: "dot_config/nvim/lua/plugins/completion.lua"
      provides: "blink.cmp completion engine"
      contains: "blink.cmp"
  key_links:
    - from: "dot_config/nvim/init.lua"
      to: "dot_config/nvim/lua/config/lazy.lua"
      via: "require('config.lazy')"
      pattern: "require.*config.lazy"
    - from: "dot_config/nvim/lua/config/lazy.lua"
      to: "LazyVim"
      via: "lazy.nvim bootstrap loading LazyVim"
      pattern: "LazyVim"
---

<objective>
Create the chezmoi directory structure and LazyVim core Neovim config -- the foundation that all subsequent plans build on.

Purpose: Establish the repo layout and a working Neovim that launches with the user's personal look and feel (gruvbox-material, keymaps, options, completion, file explorer, fuzzy finder).
Output: A `dot_config/nvim/` tree that chezmoi can apply, containing LazyVim bootstrap + core config files.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-neovim-setup/1-CONTEXT.md
@.planning/phases/01-neovim-setup/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create chezmoi directory structure and LazyVim bootstrap</name>
  <files>
    dot_config/nvim/init.lua
    dot_config/nvim/lua/config/lazy.lua
  </files>
  <read_first>
    .planning/phases/01-neovim-setup/01-RESEARCH.md (lines 170-198 for recommended project structure)
    .planning/phases/01-neovim-setup/1-CONTEXT.md (D-01, D-26 for LazyVim and chezmoi structure decisions)
  </read_first>
  <action>
    Create the chezmoi source directory structure at repo root per D-26:
    ```
    dot_config/
      nvim/
        init.lua
        lua/
          config/
            lazy.lua
            options.lua
            keymaps.lua
          plugins/
            (created in next task)
    ```

    **dot_config/nvim/init.lua** — LazyVim entry point (per D-01):
    ```lua
    -- bootstrap lazy.nvim, LazyVim and your plugins
    require("config.lazy")
    ```

    **dot_config/nvim/lua/config/lazy.lua** — lazy.nvim bootstrap + LazyVim spec:
    ```lua
    local lazypath = vim.fn.stdpath("data") .. "/lazy/lazy.nvim"
    if not (vim.uv or vim.loop).fs_stat(lazypath) then
      local lazyrepo = "https://github.com/folke/lazy.nvim.git"
      local out = vim.fn.system({ "git", "clone", "--filter=blob:none", "--branch=stable", lazyrepo, lazypath })
      if vim.v.shell_error ~= 0 then
        vim.api.nvim_echo({
          { "Failed to clone lazy.nvim:\n", "ErrorMsg" },
          { out, "WarningMsg" },
          { "\nPress any key to exit..." },
        }, true, {})
        vim.fn.getchar()
        os.exit(1)
      end
    end
    vim.opt.rtp:prepend(lazypath)

    require("lazy").setup({
      spec = {
        { "LazyVim/LazyVim", import = "lazyvim.plugins" },
        { import = "plugins" },
      },
      defaults = {
        lazy = false,
        version = false,
      },
      checker = { enabled = true },
      performance = {
        rtp = {
          disabled_plugins = {
            "gzip",
            "tarPlugin",
            "tohtml",
            "tutor",
            "zipPlugin",
          },
        },
      },
    })
    ```

    Do NOT enable any LazyVim extras in lazy.lua (especially NOT `lang.typescript` which would install vtsls instead of ts_ls per research Pitfall 2).

    No employer names, internal URLs, or credentials in any file (per D-28 / DFM-04).
  </action>
  <verify>
    <automated>test -f dot_config/nvim/init.lua && grep -q 'require.*config.lazy' dot_config/nvim/init.lua && test -f dot_config/nvim/lua/config/lazy.lua && grep -q 'LazyVim' dot_config/nvim/lua/config/lazy.lua && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - dot_config/nvim/init.lua contains `require("config.lazy")`
    - dot_config/nvim/lua/config/lazy.lua contains `"LazyVim/LazyVim"` and `import = "lazyvim.plugins"`
    - dot_config/nvim/lua/config/lazy.lua does NOT contain `lang.typescript` or `vtsls`
    - No company names, internal hostnames, or credentials in any file
  </acceptance_criteria>
  <done>LazyVim bootstrap files exist in chezmoi source structure; init.lua requires config.lazy; lazy.lua imports LazyVim and local plugins</done>
</task>

<task type="auto">
  <name>Task 2: Create core config files (options, keymaps, colorscheme, UI plugins, completion)</name>
  <files>
    dot_config/nvim/lua/config/options.lua
    dot_config/nvim/lua/config/keymaps.lua
    dot_config/nvim/lua/plugins/colorscheme.lua
    dot_config/nvim/lua/plugins/ui.lua
    dot_config/nvim/lua/plugins/completion.lua
  </files>
  <read_first>
    .planning/phases/01-neovim-setup/1-CONTEXT.md (D-02 through D-10 for all core config decisions)
    .planning/phases/01-neovim-setup/01-RESEARCH.md (lines 556-570 for blink.cmp config, lines 200-246 for LazyVim plugin extension pattern)
  </read_first>
  <action>
    **dot_config/nvim/lua/config/options.lua** — Editor options per D-04:
    ```lua
    -- Options are automatically loaded before lazy.nvim startup
    -- Default options that are always set: https://github.com/LazyVim/LazyVim/blob/main/lua/lazyvim/config/options.lua

    local opt = vim.opt

    opt.number = false          -- no line numbers
    opt.relativenumber = false  -- no relative line numbers
    opt.scrolloff = 8           -- lines of context above/below cursor
    opt.mouse = "a"             -- enable mouse in all modes
    opt.smartcase = true        -- smart case search
    opt.ignorecase = true       -- ignore case in search (needed for smartcase)
    opt.signcolumn = "yes"      -- always show signcolumn
    ```

    **dot_config/nvim/lua/config/keymaps.lua** — Custom keymaps per D-03:
    ```lua
    -- Keymaps are automatically loaded on the VeryLazy event
    -- Default keymaps: https://github.com/LazyVim/LazyVim/blob/main/lua/lazyvim/config/keymaps.lua

    local map = vim.keymap.set

    -- Scroll centering (keep cursor centered when scrolling)
    map("n", "<C-d>", "<C-d>zz", { desc = "Scroll down and center" })
    map("n", "<C-u>", "<C-u>zz", { desc = "Scroll up and center" })
    map("n", "n", "nzzzv", { desc = "Next search result centered" })
    map("n", "N", "Nzzzv", { desc = "Prev search result centered" })

    -- Telescope symbol finder (per existing personal config)
    map("n", "<leader>fs", "<cmd>Telescope lsp_document_symbols<cr>", { desc = "Document Symbols" })
    map("n", "<leader>fS", "<cmd>Telescope lsp_dynamic_workspace_symbols<cr>", { desc = "Workspace Symbols" })
    ```

    **dot_config/nvim/lua/plugins/colorscheme.lua** — gruvbox-material per D-02:
    ```lua
    return {
      {
        "sainnhe/gruvbox-material",
        lazy = false,
        priority = 1000,
        config = function()
          vim.g.gruvbox_material_background = "medium"
          vim.g.gruvbox_material_foreground = "material"
          vim.g.gruvbox_material_enable_italic = true
          vim.cmd.colorscheme("gruvbox-material")
        end,
      },

      -- Disable LazyVim's default colorscheme so gruvbox-material takes priority
      { "folke/tokyonight.nvim", enabled = false },
      { "catppuccin/nvim", enabled = false },
    }
    ```

    **dot_config/nvim/lua/plugins/ui.lua** — nvim-tree (D-08) and telescope (D-07):
    ```lua
    return {
      -- nvim-tree file explorer (per D-08)
      {
        "nvim-tree/nvim-tree.lua",
        dependencies = { "nvim-tree/nvim-web-devicons" },
        keys = {
          { "<leader>e", "<cmd>NvimTreeToggle<cr>", desc = "File Explorer" },
        },
        opts = {
          view = {
            width = 30,
          },
          renderer = {
            group_empty = true,
          },
          filters = {
            dotfiles = false,
          },
        },
      },

      -- Telescope is already included by LazyVim (per D-07)
      -- Additional telescope config if needed
      {
        "nvim-telescope/telescope.nvim",
        opts = {
          defaults = {
            layout_strategy = "horizontal",
            layout_config = {
              horizontal = {
                prompt_position = "top",
              },
            },
            sorting_strategy = "ascending",
          },
        },
      },

      -- Disable neo-tree (LazyVim default) in favor of nvim-tree
      { "nvim-neo-tree/neo-tree.nvim", enabled = false },
    }
    ```

    **dot_config/nvim/lua/plugins/completion.lua** — blink.cmp per D-05, D-06, pinned to v1 per research Pitfall 6:
    ```lua
    return {
      -- blink.cmp completion engine (per D-05, D-06)
      {
        "saghen/blink.cmp",
        version = "1.*",  -- pin to v1 stable, avoid V2 breaking changes
        opts = {
          sources = {
            default = { "lsp", "path", "snippets", "buffer" },
          },
        },
      },
    }
    ```

    Per D-10 / DIAG-01 conflict: do NOT add trouble.nvim. DIAG-01 is satisfied by Neovim's built-in inline LSP diagnostics and hover (D-10 locked decision overrides REQUIREMENTS.md).
  </action>
  <verify>
    <automated>grep -q 'scrolloff' dot_config/nvim/lua/config/options.lua && grep -q 'vim.keymap.set' dot_config/nvim/lua/config/keymaps.lua && grep -q 'gruvbox-material' dot_config/nvim/lua/plugins/colorscheme.lua && grep -q 'nvim-tree' dot_config/nvim/lua/plugins/ui.lua && grep -q 'blink.cmp' dot_config/nvim/lua/plugins/completion.lua && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - options.lua contains `opt.number = false` and `opt.scrolloff = 8` and `opt.mouse = "a"` and `opt.smartcase = true` and `opt.signcolumn = "yes"`
    - keymaps.lua contains `<C-d>` scroll centering and `<leader>fs` telescope symbol finder
    - colorscheme.lua contains `sainnhe/gruvbox-material` and `vim.cmd.colorscheme("gruvbox-material")`
    - colorscheme.lua disables tokyonight and catppuccin (LazyVim defaults)
    - ui.lua contains `nvim-tree/nvim-tree.lua` and `nvim-telescope/telescope.nvim`
    - ui.lua disables neo-tree (`enabled = false`)
    - completion.lua contains `saghen/blink.cmp` with `version = "1.*"` and sources `{ "lsp", "path", "snippets", "buffer" }`
    - No trouble.nvim in any file (per D-10)
    - No company names, internal hostnames, or credentials in any file
  </acceptance_criteria>
  <done>Core Neovim config complete: options match personal setup, keymaps ported, gruvbox-material colorscheme active, nvim-tree and telescope configured, blink.cmp completion with 4 sources</done>
</task>

</tasks>

<verification>
- All files exist under dot_config/nvim/ in chezmoi source structure
- `grep -rn 'gruvbox-material' dot_config/` confirms colorscheme
- `grep -rn 'scrolloff' dot_config/` confirms options
- `grep -rn 'blink.cmp' dot_config/` confirms completion engine
- `grep -rn 'trouble' dot_config/` returns nothing (D-10 honored)
- `grep -rn 'lang.typescript' dot_config/` returns nothing (Pitfall 2 avoided)
- No grep hits for company names or internal URLs across all files
</verification>

<success_criteria>
- chezmoi source structure exists: dot_config/nvim/init.lua, lua/config/{lazy,options,keymaps}.lua, lua/plugins/{colorscheme,ui,completion}.lua
- LazyVim bootstrap is correct (imports LazyVim plugins + local plugins directory)
- No LazyVim extras enabled that would conflict with later plans (no lang.typescript)
- All 7 files have correct content matching locked decisions D-01 through D-10
</success_criteria>

<output>
After completion, create `.planning/phases/01-neovim-setup/01-01-SUMMARY.md`
</output>
